-- Local development seed data only. Never run against a production
-- project. Populated further in Phase 2+ once groups/memberships exist.

-- Example: after creating a user via `supabase auth` locally or the
-- Studio UI, you can give them a friendlier display name for testing:
-- update profiles set display_name = 'Ada Test' where user_id = '<uuid>';
